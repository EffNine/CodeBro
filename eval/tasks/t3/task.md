# T3 session A — implement steps 1–2, then STOP

In the crate `registry`, implement `TaskRegistry` per `spec.md`, **steps 1–2
only** (`new`, `add`, `get`, `list`, `remove`).

## Done when

- `cargo test step12` is fully green.
- Do NOT implement steps 3–4 (`save`, `load`, `rename` may stay
  `unimplemented!()`).
- Do not change the public API signatures in `spec.md`.

Then stop. A later session will continue from your on-disk state.
