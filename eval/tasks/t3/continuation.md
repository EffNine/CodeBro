# T3 session B — finish steps 3–4

Continue the work in the crate `registry`. Read `spec.md` and the current
source code to see what already exists.

Complete steps 3–4 per `spec.md` (`save`, `load`, `rename`, validation rules).

## Done when

- Full `cargo test` is green (steps 1–2 keep passing).
- Do not change the public API signatures in `spec.md`.
